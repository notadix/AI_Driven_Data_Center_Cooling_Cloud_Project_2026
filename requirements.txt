# AI-Driven Cooling Digital Twin — backend & AI runtime dependencies.
# Generated from actual imports across src/, database/, dataset/, testing/,
# pinned to the versions this repo's test suite (see docs/FINAL_QA.md for the current count) runs against.

# Backend / API
fastapi>=0.138.0
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
prometheus_client>=0.20.0  # /metrics endpoint (COOLING_REGISTRY, scrape-time gauges)

# AWS SDK (IoT Core, Timestream, SiteWise, SageMaker, Step Functions, EventBridge, SNS)
boto3>=1.43.97
botocore>=1.43.97
pyjwt[crypto]>=2.10.0    # verifies Cognito ID tokens against the pool's JWKS (src/backend/services/cognito_auth.py)

# AI / ML
torch>=2.12.1
gymnasium>=1.3.0
numpy>=2.5.0
scikit-learn>=1.9.0
scipy>=1.14.0            # calibration fit, carbon-aware scheduler LP
matplotlib>=3.9.0        # scripts/make_result_charts.py

# Data pipeline
pandas>=3.0.5
pyarrow>=25.0.1
openpyxl>=3.1.0         # dataset/download_dataset.py reads the Frontier2023 .xlsx workbook

# Test suite
pytest>=9.1.1
httpx>=0.28.1

# Optional (not needed to run the app or the tests): the SageMaker pipeline definition imports `sagemaker`
# lazily inside get_pipeline(); install it only if you build the pipeline against a real AWS account.
# sagemaker>=2.200.0
