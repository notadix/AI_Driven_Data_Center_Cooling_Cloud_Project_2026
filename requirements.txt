# AI-Driven Cooling Digital Twin — backend & AI runtime dependencies.
# Generated from actual imports across src/, database/, dataset/, testing/,
# pinned to the versions this repo's test suite (135 passed, 1 skipped) runs against.

# Backend / API
fastapi>=0.138.0
uvicorn[standard]>=0.49.0
pydantic>=2.13.4

# AWS SDK (IoT Core, Timestream, SiteWise, SageMaker, Step Functions, EventBridge, SNS)
boto3>=1.43.97
botocore>=1.43.97

# AI / ML
torch>=2.12.1
gymnasium>=1.3.0
numpy>=2.5.0
scikit-learn>=1.9.0

# Data pipeline
pandas>=3.0.5
pyarrow>=25.0.1

# Test suite
pytest>=9.1.1
httpx>=0.28.1
